# HA Entity Resolver

Smart entity resolution addon for Home Assistant with fuzzy matching support.

## Features

- 🔍 Fuzzy matching for natural language queries
- 🏠 Supports HA areas, devices, and entities hierarchy
- 🌐 Vietnamese text normalization
- 🔐 Secure authentication using HA long-lived tokens
- 📊 Entity metadata included in response

## Configuration

```yaml
log_level: info
ha_token: "your_long_lived_token_here"
